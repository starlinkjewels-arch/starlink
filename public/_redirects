# Redirect www to non-www (fixes duplicate content / redirect errors in Google Search Console)
https://www.starlinkjewels.com/* https://starlinkjewels.com/:splat 301!

# SPA fallback — serve index.html for all client-side routes
/*  /index.html  200
